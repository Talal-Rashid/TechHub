<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/app_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="84dp"
        android:gravity="bottom|center_vertical"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="14dp"
        android:background="@color/amber">

        <TextView
            android:id="@+id/topBarTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Admin"
            android:textColor="@color/black"
            android:textSize="22sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/btnProfile"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@android:drawable/ic_menu_manage"
            android:tint="@color/black" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <GridLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:columnCount="2">

                <LinearLayout
                    android:id="@+id/cardVendorApprovals"
                    style="@style/HomeSquareCard">
                    <TextView style="@style/HomeCardTitle" android:text="Vendors" />
                    <TextView style="@style/HomeCardSubtitle" android:text="Approve sellers" />
                </LinearLayout>

                <LinearLayout
                    android:id="@+id/cardAdminOrders"
                    style="@style/HomeSquareCard">
                    <TextView style="@style/HomeCardTitle" android:text="Orders" />
                    <TextView style="@style/HomeCardSubtitle" android:text="All orders" />
                </LinearLayout>

                <LinearLayout
                    android:id="@+id/cardAdminProducts"
                    style="@style/HomeSquareCard">
                    <TextView style="@style/HomeCardTitle" android:text="Products" />
                    <TextView style="@style/HomeCardSubtitle" android:text="Moderation" />
                </LinearLayout>

            </GridLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Pending Vendor Approvals"
                android:textColor="@color/primary_text"
                android:textSize="20sp"
                android:textStyle="bold"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewPendingUsers"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>