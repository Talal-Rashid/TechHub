<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/app_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="84dp"
        android:gravity="bottom|center_vertical"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="14dp"
        android:background="@color/amber">

        <TextView
            android:id="@+id/topBarTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Vendor"
            android:textColor="@color/black"
            android:textSize="22sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/btnProfile"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@android:drawable/ic_menu_manage"
            android:tint="@color/black" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:padding="16dp">

            <LinearLayout
                android:id="@+id/cardAddProduct"
                style="@style/HomeSquareCard">
                <TextView style="@style/HomeCardTitle" android:text="Add Product" />
                <TextView style="@style/HomeCardSubtitle" android:text="Create listing" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/cardManageProducts"
                style="@style/HomeSquareCard">
                <TextView style="@style/HomeCardTitle" android:text="Products" />
                <TextView style="@style/HomeCardSubtitle" android:text="Manage stock" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/cardVendorOrders"
                style="@style/HomeSquareCard">
                <TextView style="@style/HomeCardTitle" android:text="Orders" />
                <TextView style="@style/HomeCardSubtitle" android:text="Customer orders" />
            </LinearLayout>

        </GridLayout>
    </ScrollView>

</LinearLayout>